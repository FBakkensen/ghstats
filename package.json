{
  "name": "ghstats",
  "version": "0.1.0",
  "description": "GitHub activity dashboard with CLI and OpenTUI front-ends",
  "type": "module",
  "main": "src/cli/index.ts",
  "bin": {
    "ghstats": "./bin/ghstats"
  },
  "scripts": {
    "start": "bun run src/cli/index.ts",
    "dev": "bun run --hot src/cli/index.ts",
    "test": "bun test",
    "build": "bun build src/cli/index.ts --outfile=dist/ghstats"
  },
  "dependencies": {
    "@octokit/graphql": "^7.0.0",
    "@octokit/rest": "^20.0.0",
    "@opentui/core": "^0.1.0",
    "commander": "^12.0.0",
    "date-fns": "^3.0.0"
  },
  "devDependencies": {
    "bun-types": "latest"
  },
  "engines": {
    "bun": ">=1.0.0"
  }
}
